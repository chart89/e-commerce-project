import { Container } from "react-bootstrap";
import styles from './Search.module.scss';

const Search = () => {
    return (
        <Container className={`text-center my-5 ${styles.searchDiv}`}>
            <h1>Under construction...</h1>
        </Container>
        
    );
};

export default Search